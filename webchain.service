
[Unit]
Description=Webchain
After=network.target
[Service]
Type=forking
GuessMainPID=no
ExecStart= webchain -c /etc/webchain/config.json -l /var/log/webchain/miner.log -B
Restart=always
User=root
[Install]
WantedBy=multi-user.target
